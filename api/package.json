{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "description": "Serverless API functions for Lingua Stagify",
  "scripts": {
    "dev": "echo 'API functions ready - use Vercel CLI from root for local testing'",
    "build": "echo 'Serverless functions ready for deployment'",
    "lint": "echo 'No lint configured for API'",
    "db:push": "npx prisma db push",
    "db:seed": "node prisma/seed.js",
    "db:seed:simple": "node prisma/simple-seed.js",
    "db:studio": "npx prisma studio",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "google-auth-library": "^10.1.0",
    "express": "^4.21.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  },
  "engines": {
    "node": ">=20.x"
  }
}
